# Job-Automation-Macro-Intelligence
Automated Interview Preperation
# interview_prep — TTS Interview Audio Generator

This scripts turns a long-form script into an MP3 using `pyttsx3`. It is offline, local TTS (no cloud API keys).

Usage:
1. Create venv, install requirements.
2. Edit `sample_script.txt` or edit `tts_prep.py`.
3. Run: `python tts_prep.py`
Output: `defi_loop.mp3`
